## Customize the test machine
machine:

  timezone:
    UTC # Set the timezone to UTC

  # Version of python to use
  python:
    version: 2.7.5

## Customize test commands
test:
  override:
    # run the auto_exam bash script to test examples automatically
    - bash -c "sh auto_exams"